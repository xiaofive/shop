spring:
  application:
    name: service-jyzt-consumer
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:121.196.50.221}:${REGISTER_PORT:8848}
        namespace: jyzt-cloud-staging

      config:
        server-addr: ${REGISTER_HOST:121.196.50.221}:${REGISTER_PORT:8848}
        namespace: jyzt-cloud-staging
        file-extension: yml
        group: JYZT-CLOUD
