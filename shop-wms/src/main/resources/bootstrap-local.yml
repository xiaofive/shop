spring:
  application:
    name: shop-wms
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:1.116.205.169}:${REGISTER_PORT:8848}
        namespace: shop-product-local

      config:
        server-addr: ${REGISTER_HOST:1.116.205.169}:${REGISTER_PORT:8848}
        namespace: shop-product-local
        file-extension: yml
        group: SHOP
